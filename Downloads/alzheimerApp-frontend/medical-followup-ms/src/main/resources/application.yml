server:
  port: 8081

spring:
  application:
    name: medical-followup-ms

  datasource:
    url: jdbc:mysql://localhost:3306/medical_followup?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update   # dev only (plus tard -> validate + Flyway)
    properties:
      hibernate:
        format_sql: true
    show-sql: true
